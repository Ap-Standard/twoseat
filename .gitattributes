* text=auto eol=lf

# The action ships a bundled runtime. It is generated by `npm run build` and
# verified against source by the dist-drift job in CI, so collapse it in diffs.
dist/** linguist-generated=true
package-lock.json linguist-generated=true
