{
  "id": "clean-006",
  "kind": "clean",
  "category": "none",
  "description": "A nullable column added on its own. Resembles unsafe-migration and is the safe form of one.",
  "files": [
    {
      "path": "migrations/0046_add_note.sql",
      "patch": [
        "@@ -1,0 +1,3 @@",
        "+-- Nullable, so the add takes no rewrite and holds no long lock.",
        "+-- The backfill and the NOT NULL constraint ship as separate migrations.",
        "+ALTER TABLE tickets ADD COLUMN note text;"
      ]
    }
  ],
  "expected": []
}
