{
  "id": "clean-003",
  "kind": "clean",
  "category": "none",
  "description": "A read-modify-write is moved inside a locking transaction. Resembles toctou and is the fix for one.",
  "files": [
    {
      "path": "src/billing/credit.ts",
      "patch": [
        "@@ -22,4 +22,6 @@",
        " export async function credit(db: Db, accountId: string, amount: number) {",
        "-  const account = await db.accounts.find(accountId);",
        "-  await db.accounts.setBalance(accountId, account.balance + amount);",
        "+  return db.transaction(async (tx) => {",
        "+    const account = await tx.accounts.findForUpdate(accountId);",
        "+    await tx.accounts.setBalance(accountId, account.balance + amount);",
        "+  });",
        " }"
      ]
    }
  ],
  "expected": []
}
